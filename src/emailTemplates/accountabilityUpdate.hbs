<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tradeboard - Trading Accountability Update</title>
    <style>
        :root {
            --primary-color: #8B5CF6;
            --primary-light: #EDE9FE;
            --text-dark: #1F2937;
            --text-light: #6B7280;
            --success-color: #10B981;
            --warning-color: #F59E0B;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #F3F4F6;
        }

        .container {
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            overflow: hidden;
        }

        .header {
            background-color: var(--primary-color);
            color: white;
            padding: 24px;
            text-align: center;
        }

        .header h1 {
            font-size: 24px;
            font-weight: 600;
            margin: 0;
        }

        .content {
            padding: 32px 24px;
        }

        .greeting {
            margin-bottom: 24px;
            color: var(--text-dark);
        }

        .metrics-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 16px;
            margin-bottom: 24px;
        }

        .metric {
            background-color: var(--primary-light);
            padding: 16px;
            border-radius: 12px;
            transition: transform 0.2s ease;
        }

        .metric:hover {
            transform: translateY(-2px);
        }

        .metric-name {
            font-weight: 600;
            color: var(--primary-color);
            display: block;
            margin-bottom: 4px;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .metric-value {
            font-size: 20px;
            font-weight: 600;
            color: var(--text-dark);
        }

        .date-range-metrics {
            background-color: white;
            border: 1px solid #E5E7EB;
            border-radius: 12px;
            padding: 20px;
            margin-top: 24px;
        }

        .date-range-metrics ul {
            list-style: none;
            margin-top: 12px;
        }

        .date-range-metrics li {
            padding: 8px 0;
            border-bottom: 1px solid #E5E7EB;
        }

        .date-range-metrics li:last-child {
            border-bottom: none;
        }

        .message {
            margin-top: 24px;
            padding: 16px;
            background-color: var(--primary-light);
            border-radius: 12px;
            color: var(--primary-color);
            font-weight: 500;
        }

        .footer {
            text-align: center;
            margin-top: 32px;
            padding: 16px;
            color: var(--text-light);
            font-size: 0.875rem;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Trading Accountability Update</h1>
        </div>
        <div class="content">
            <div class="greeting">
                <p>Hello {{partnerName}},</p>
                <p>Here's the latest update on your trading accountability partner's performance:</p>
            </div>

            <div class="metrics-grid">
                {{#if sharedData.capital}}
                <div class="metric">
                    <span class="metric-name">Current Capital</span>
                    <div class="metric-value">₹{{sharedData.capital}}</div>
                </div>
                {{/if}}

                {{#if sharedData.rulesFollowed}}
                <div class="metric">
                    <span class="metric-name">Rules Followed</span>
                    <div class="metric-value">{{sharedData.rulesFollowed}}</div>
                </div>
                {{/if}}

                {{#if sharedData.winRate}}
                <div class="metric">
                    <span class="metric-name">Win Rate</span>
                    <div class="metric-value">{{sharedData.winRate}}%</div>
                </div>
                {{/if}}

                {{#if sharedData.tradesTaken}}
                <div class="metric">
                    <span class="metric-name">Trades Taken</span>
                    <div class="metric-value">{{sharedData.tradesTaken}}</div>
                </div>
                {{/if}}

                {{#if sharedData.profitLoss}}
                <div class="metric">
                    <span class="metric-name">Profit/Loss</span>
                    <div class="metric-value">₹{{sharedData.profitLoss}}</div>
                </div>
                {{/if}}

                {{#if sharedData.currentPoints}}
                <div class="metric">
                    <span class="metric-name">Current Points</span>
                    <div class="metric-value">{{sharedData.currentPoints}}</div>
                </div>
                {{/if}}
            </div>

            {{#if sharedData.dateRangeMetrics}}
            <div class="date-range-metrics">
                <span class="metric-name">Date Range Metrics</span>
                <ul>
                    <li>
                        <strong>Profit Days:</strong> {{sharedData.dateRangeMetrics.profit_days.avgTradesTaken}} trades,
                        {{sharedData.dateRangeMetrics.profit_days.winRate}}% win rate
                    </li>
                    <li>
                        <strong>Loss Days:</strong> {{sharedData.dateRangeMetrics.loss_days.avgTradesTaken}} trades,
                        {{sharedData.dateRangeMetrics.loss_days.winRate}}% win rate
                    </li>
                    <li>
                        <strong>Break-Even Days:</strong> {{sharedData.dateRangeMetrics.breakEven_days.avgTradesTaken}}
                        trades,
                        {{sharedData.dateRangeMetrics.breakEven_days.winRate}}% win rate
                    </li>
                </ul>
            </div>
            {{/if}}

            {{dataViewLink}}

            <div class="message">
                Keep encouraging your partner to maintain discipline and follow their trading plan. Your support makes a
                difference!
            </div>
        </div>
    </div>
    <div class="footer">
        <p>&copy; 2024 Tradeboard. All rights reserved.</p>
    </div>
</body>

</html>