<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tradeboard Email Templates</title>
    <style>
        :root {
            --primary-color: #8B5CF6;
            --primary-light: #EDE9FE;
            --text-dark: #1F2937;
            --text-light: #6B7280;
            --accent-color: #7C3AED;
            --success-green: #10B981;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: var(--text-dark);
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: #F3F4F6;
        }

        .container {
            background-color: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            margin-bottom: 40px;
        }

        .header {
            background-color: var(--primary-color);
            color: white;
            padding: 32px 24px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .header::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 40px;
            background: linear-gradient(135deg, var(--primary-color) 25%, var(--accent-color) 100%);
            opacity: 0.3;
        }

        .header h1 {
            font-size: 28px;
            font-weight: 600;
            margin: 0;
            position: relative;
            z-index: 1;
        }

        .content {
            padding: 32px 24px;
        }

        .welcome-text {
            text-align: center;
            margin-bottom: 24px;
            color: var(--text-dark);
            font-size: 18px;
            line-height: 1.6;
        }

        .feature-box {
            background-color: var(--primary-light);
            padding: 24px;
            border-radius: 12px;
            margin: 24px 0;
        }

        .otp-code {
            font-size: 32px;
            font-weight: 600;
            letter-spacing: 4px;
            color: var(--primary-color);
            text-align: center;
            padding: 16px;
            background-color: var(--primary-light);
            border-radius: 12px;
            margin: 24px 0;
        }

        .note {
            font-size: 14px;
            color: var(--text-light);
            text-align: center;
            margin-top: 16px;
        }

        .cta-button {
            display: block;
            width: 100%;
            max-width: 300px;
            margin: 32px auto;
            padding: 16px 24px;
            background-color: var(--primary-color);
            color: white;
            text-decoration: none;
            text-align: center;
            border-radius: 8px;
            font-weight: 600;
            transition: background-color 0.2s ease;
        }

        .cta-button:hover {
            background-color: var(--accent-color);
        }

        .footer {
            text-align: center;
            padding: 16px;
            color: var(--text-light);
            font-size: 0.875rem;
            border-top: 1px solid #E5E7EB;
        }

        .highlights {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
            margin: 24px 0;
        }

        .highlight-item {
            background-color: #F9FAFB;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid #E5E7EB;
        }

        .highlight-item h3 {
            color: var(--primary-color);
            margin-bottom: 8px;
            font-size: 16px;
        }

        .warning {
            background-color: #FEF3C7;
            border-left: 4px solid #F59E0B;
            padding: 16px;
            border-radius: 8px;
            margin: 24px 0;
            font-size: 14px;
            color: #92400E;
        }
    </style>
</head>

<body>

    <!-- OTP Email Template -->
    <div class="container">
        <div class="header">
            <h1>Verify Your Account</h1>
        </div>
        <div class="content">
            <p class="welcome-text">Hello {{userName}},<br>Please use the following OTP to verify your account.</p>

            <div class="otp-code">
                {{otpCode}}
            </div>

            <div class="warning">
                This OTP will expire in 10 minutes. Do not share this code with anyone.
            </div>

            <div class="feature-box">
                <p>If you didn't request this verification code, please ignore this email or contact support if you have
                    concerns.</p>
            </div>

            <p class="note">This is an automated message, please do not reply to this email.</p>
        </div>
        <div class="footer">
            <p>&copy; 2024 Tradeboard. All rights reserved.</p>
        </div>
    </div>
</body>

</html>